---
- name: "install dependencies."
  become: true
  apt:
    name: ["nodejs","npm","unzip"]
    state: present
    update_cache: yes

- name: Install pm2
  become: true
  npm:
    name: pm2
    global: yes
    production: yes
    state: present

- name: Extract archive
  unarchive:
    src: artifact.tar.gz
    dest: /home/ubuntu

- name: Executing node
  become: true
  shell: |
    cd /home/ubuntu/backend
    npm install
    pm2 stop default
    pm2 start npm -- start
